# 当前任务与状态

> 最后更新：2026-09-06

## 当前任务

当前推进 T09 上线准备；下面 2026-09-01 的 MVP/策略条目保留为历史记录，不代表最新检索策略或本次上线验收。

- 当前任务包：`02-任务/01-current/T09-上线准备/`
- 状态：代码侧发布准备完成、待验收与目标环境验证；部署手册在任务包 `工作产出/上线部署手册.md`。
- 验证：13 项发布测试、24 项 API 测试、60 项编译后 Worker 测试、完整入口类型检查及发布构建通过；独立编译 API 冒烟通过。不代表真实检索/生产环境验收。
- 上线阻断：依赖审计 8 high/2 moderate；域名/备案、SMTP、国内网络、生产迁移和恢复演练待完成。未生产部署、未改密钥、未重启当前任务。
- UI 基准：Figma `媒介助手｜高保真原型 / Core Screens`
- 产品基线：`03-交付物/01-产品文档/01-current/媒介助手_MVP_PRD.md`

## 当前状态

| 环节 | 状态 | 说明 |
| --- | --- | --- |
| 产品定义/PRD/原型/技术方案/API 契约 | 已完成 | 全部进入 current |
| 数据库实施 | 已完成 | 核心迁移、Storage 与 `202608310001` 私有 RedFox 凭据表均已推送 |
| 技术验证 | 通过 | T06 连接（D012/D014）、T07 检索主链路（D015）、T08 知识库+审核主链路（D016）全部用户验证通过 |
| Vibe Coding | T01–T08 全部完成并归档 | 前端、连接/扫码、达人检索、知识库、内容审核全链路真实数据闭环 |
| 会话导入 | 已完成 | Codex 主线对话已导入 |
| Planner/Agent 管理面一致性 | 已完成 | 13 个达人 Skill、10 个审核 Skill；真实 Tool/模型/执行清单 |
| 达人检索 20 人兜底 | 已完成 | 模型 `limit` 强制归一为 20；32 路基础/长尾/宽召回；自动续检至达标或真实终止条件 |
| 达人检索质量策略 | 已完成 | 首轮覆盖 8 个不同主题页；统一统计粉丝、近期点赞、近 30 天发帖数、最近发帖时间四类硬条件，只放宽淘汰最多的一项；其余硬条件和内容语义严格校验后按综合评分取前 20 |
| RedFox API Key 替换 | 已完成 | 用户级 Key 服务端 AES-256-GCM 加密、仅指纹回显；个人 Key 优先于系统 Key；余额不足可直接替换并沿保存游标继续 |
| Worker 新版本加载 | 进行中 | 当前唯一 Worker PID 21052 正在处理已接管的真实任务；该任务完成后安全重载最终版本，避免中途重试造成 RedFox 重复消耗；全量测试 64/64、类型检查、Lint 和生产构建通过 |

## 下一步（可选）

1. **正式验收报告**：汇总 D012–D016 到 `03-交付物/04-测试与验收/01-current/`（MVP 阶段验收文档）。
2. **本地 Eval 已建立**（2026-08-19）：`npm run eval:server` → `http://127.0.0.1:3081`（独立端口，D005 仅本地不部署）。rule_parse 10/10、audit_rules 12/12（精确率/召回率 1.0）、semantic_match 基线 2/2。可扩展：semantic_match 期望集人工标注回填、审核规则扩充、知识库检索（Q003 后）。
3. **可选增强**（按需）：完整广告法官方文本入库；配置 `OPENAI_API_KEY` 启用 LLM 语义/图片 OCR；Embedding 与中文检索 Eval（Q003）；频控/批量/并发稳定性验证；分组/批量更新路由。
4. 本地 Eval（D005 边界）仅本地运行，不作为线上功能。

## 禁止事项

1. 未经用户验收的成果不得进入任何 `01-current/`。
2. 一次只推进一个任务包，范围外发现记入待确认事项。
3. 不得存储账号密码、Cookie、Token、验证码或 API Key。
4. 不得用静态样本冒充实时小红书数据链路。
5. 本地 Eval 不得作为对外系统页面或线上功能部署。
