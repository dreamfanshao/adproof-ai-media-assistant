# DSH 会话交接 · 2026-08-18 ~ 08-19（媒介助手 MVP 完成期）

> 用途：让 Codex（或任意后续会话）无痕接续 2026-08-18~19 在 DSH（DeepSeek Harness Web，http://127.0.0.1:3080）中完成的工作。
> 交接日期：2026-08-19
> 前置交接：`02-任务/99-done/T06-小红书扫码与真实读取PoC/工作产出/Codex会话导入与开发交接.md`（Codex 08-11~18 会话已导入）。

## 1. Codex 入口（读序）

1. `docs/project-docs/99-工作区索引.md`（当前阶段：MVP 两大主链路完成，T01–T08 已归档）
2. `docs/project-docs/00-工作台/当前任务与状态/当前任务与状态.md`（无进行中任务，下一步为可选增强）
3. `docs/project-docs/00-工作台/决策与待确认事项/决策与待确认事项.md`（D001–D016）
4. `docs/project-docs/09-工作日志/2026-08-18.md` 与 `2026-08-19.md`（本会话逐条记录）
5. 相关任务包验收证据（`02-任务/99-done/T07|T08/验收证据/`）

## 2. 本会话完成事项

| 日期 | 事项 | 产物 |
| --- | --- | --- |
| 08-18 | **T07 达人检索主链路**（Phase 1–4）：真实检索读取/字段映射、Worker 执行器（pg 直连 private.jobs）、语义匹配（启发式+确定性 match_score）、API+SSE、前端真实接入、Excel 导出；41 个真实达人落库；验收归档（D015） | `worker/src/`（xhs-field-mapper / xhs-db / xhs-search-executor / xhs-semantic-matcher / xhs-hidden-browser / index.ts）、`server/src/`（rule-parser / creator-search-service / routes: search|creators|projects|task-events）、`src/lib/project-api.ts` 等 |
| 08-18 | **T08 知识库+内容审核主链路**（Phase 1–4）：知识库入库（签名上传/解析/切块/状态机）、广告法公共库 seed、审核流水线（规则匹配 v1：绝对化/医疗断言/引证无出处/虚构评价 → risk 项 → overall_risk 合成 → 快照）、前端三页真实接入；验收归档（D016） | `server/src/`（knowledge-service / audit-service / audit-rules / routes: knowledge|audit）、`worker/src/knowledge-ingest-executor.ts`、`worker/src/audit-executor.ts`、`src/pages/`（knowledge / new-audit / audit-result / audit-history） |
| 08-18 | 测试素材：小红书社区规范 Word（`01-输入素材/02-法规与规则/小红书社区规范.docx`）、违规图文测试包（`01-输入素材/03-测试数据/审核测试_违规图文.md` + 配图 PNG） | 已登记素材清单 |
| 08-18 | 修复：结果页轮询中间态、"图片不能拖拽"、审核快照先于发现项（触发器约束）等 | 见工作日志 |
| 08-19 | **本地 Eval「运营与质量中心」**：`npm run eval:server` → http://127.0.0.1:3081（独立端口，D005 不部署）。三套件（rule_parse/audit_rules/semantic_match）、评分方法（精准/余弦 bigram/LLM-judge 可编辑 prompt/人工六维）、数据标注工作台（六维+状态+JSONL 导入导出）、人工评估、改进建议中心、评测集/评测中心（bad case 可视） | `eval/`（datasets / runner / server / storage / public） |
| 08-19 | 解析器三处修复（Eval 首轮 83.3%→100%）："不超过"→lte、混合子句语义词提取、否定语义 match:none | `server/src/services/rule-parser.ts` |

## 3. 当前运行环境

- 主系统：API `http://127.0.0.1:3001`（Fastify + pg 直连）、前端 `http://127.0.0.1:5173`（Vite）、Worker 守护进程（creator_search / knowledge_ingest / content_audit）。
- Eval 中心：`http://127.0.0.1:3081`（独立，不部署）。
- 密钥/配置均在 `.env.local`（gitignore）：`SUPABASE_SERVICE_ROLE_KEY`、`XHS_SESSION_ENCRYPTION_KEY`、`DATABASE_URL`（Supabase 数据库连接串，Q004 已解决）；`OPENAI_API_KEY` **未配置**。
- 自检：`npm run typecheck`、`npm run test:server`（14/14）、Eval 24/24。

## 4. 未做待办（按优先级）

- **P0**：工作台首页（/dashboard）仍为 demo（未接真实统计）；正式验收报告（`03-交付物/04-测试与验收/01-current/` 为空）；部署与使用说明（`03-交付物/05-部署与使用说明/` 为空）。
- **P1**：完整《广告法》官方文本入库（现为核心条款 seed，待用户提供）；semantic_match 期望集人工标注回填（Eval 工作台已建）；审核规则扩充（小红书社区规范导流/医美高风险规则）；达人 Eval 标注样本（≥100 条建议）。
- **P2**：LLM/OCR 启用（OPENAI_API_KEY）；Embedding（Q003 延后）；频控/批量/并发验证；分组/批量更新路由（groups/bulk-update 契约已定义未实现）；原型设计交付物；Eval 参考图剩余页签（版本回滚/Prompt A/B/运行监控）；旧 service_role/anon key 停用（需用户控制台操作）。

## 5. 完整会话转录导出（可选，需工具）

- DSH 会话存储：`C:\Users\wff\.dsh\sessions\--E-code-AICoding-agent_medium--\session-140664c1-73a6-4ae5-94e2-16810c72ab5d\session.jsonl.zstd`（zstd 压缩）。
- 本机无 zstd CLI / python；`zstddec`/`zstd-codec`（WASM）对 DSH 格式解码异常（输出乱码）。若要逐字转录，需先安装 zstd（如 `winget install zstd` 或用带 zstd 的 7-Zip 解压后按行转 Markdown）。
- 建议：以本文档 + 工作区文档体系作为 Codex 主交接通道（与之前 Codex→DSH 交接同构），完整转录仅作审计/回溯补充。
