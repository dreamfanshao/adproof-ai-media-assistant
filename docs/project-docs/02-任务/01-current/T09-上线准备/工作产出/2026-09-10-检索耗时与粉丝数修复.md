# 检索耗时与粉丝数修复验证

## 问题与证据

- 任务 `63bf699a-23a7-43e5-98e2-ebd4b972f434`：2026-09-10 10:43:14Z 至 11:33:40Z，约 50 分 26 秒，返回 20 人。
- 账号详情 129 次调用中，96 次返回优质库未收录。实时作品召回与优质库账号详情混用，使小账号资料缺失。
- 前端将 `followers=null` 转成 `0`，不是不足 1000 的数字截断。
- 模型有 22 次 30 秒超时；候选 AI 分析串行。旧耗时日志只记录收到响应头的时间，漏掉生成正文的等待。

## 修复

1. 实时检索使用 `/story/api/xhs/ability/accountDetail` 按 `userId` 补全资料；优质库检索保留原路由。依据 [RedFox 官方接口契约](https://doc.redfox.hk/474466918e0.md)，读取真实 `fansCount`。
2. 未知粉丝数显示“未知”，真实 0 仍显示 0；不足 1000 的数字正常展示。未知在升序、降序中都排最后。
3. 资料及 AI 使用最多 4 项有序预取；保留请求间隔、粉丝前置过滤、语义质量门槛、顺序入库、游标和取消/租约检查，不缩减目标人数或虚构通过。
4. 修复并发请求同时发出的限速竞争；账号缓存按 endpoint/base URL 隔离，已知未收录短期负缓存。
5. 检索综合语义判断默认超时 60 秒，仍可由 `CREATOR_LLM_TIMEOUT_MS` / `LLM_TIMEOUT_MS` 显式覆盖；其他模型调用默认不变。日志包含响应正文耗时。

## 数据修复

`scripts/repair-search-followers.ts` 默认 dry-run，显式任务 UUID 和 `--apply` 才写入。只补该任务仍为空的粉丝数，并核验返回账号 ID，不改选择状态、匹配分或已有非空粉丝数。

10 个缺失值已补回，包括 hi小狗 102、花花 260、Jolan 靖岚日常 769、小饭的脂皮日记 151、有些事太聪明 147、奈奈 97。数据库复查：20 人、缺失 0、零值 0、1～999 粉丝 10 人。

## 自测

- Worker 单测 64/64：包含并发上限、顺序消费、停止后不扩展任务、异常处理、实时路由、并发请求间隔、缓存、0/null/147/769/999 数值。
- API 单测 25/25。
- 按 webapp-testing 的浏览器验证流程，使用项目已有 Node Playwright 和实际 CreatorTable/mapProjectCreator 的隔离 fixture，验证未知/真实 0/147/769/1291、双向排序、无浏览器异常。不登录、不插入业务测试数据。
- Typecheck、Lint、生产 build、git diff --check 通过；build 仍有既有的前端 chunk 大于 500 kB 提示。
- `scripts/benchmark-creator-search.ts` 有界真实调用：8 个已有候选，真实账号详情和同一语义分析链，不新建任务或覆盖匹配结果。30 秒超时测试：78.8 秒，7/8 完成；调整后：90.7 秒，8/8 完成，8 次资料请求成功、无重试。一个超过 54 秒的模型响应成功完成，没有被误判失败。
- 第二轮各候选耗时合计 274.1 秒、并发墙钟 90.7 秒，验证等待发生重叠；不是严格串行 A/B 测试，不承诺整批固定提速倍数。

## 运行与边界

- 本地 Worker 已重新启动，版本 `2026-09-10.realtime-profile-parallel-v1`。
- 没有修改 `.env`、密钥或 CI/CD，没有提交/推送，没有部署 ECS。
- 未重新执行完整 20 人检索；完整耗时取决于数据源覆盖率、候选命中率和模型响应，需下一次完整任务观察。
