# T08 知识库与内容审核主链路 · 验收记录

> 验证日期：2026-08-18（自动化/集成验证），2026-08-19（用户真人验证通过）
> 结论：知识库入库（Workflow C）与图文内容合规辅助审核（Workflow D，规则匹配 v1）主链路验证通过；前端已切真实 API。
> 前置：T06（D014）、T07（D015）已验收；数据库 001–006 + Storage 迁移已推送。

## 验证环境

- API `:3001`（Fastify + pg 直连）、前端 `:5173`、Worker 守护进程（creator_search / knowledge_ingest / content_audit 三型 job）。
- 数据库：Supabase `medium-ai-dev`；Storage：`knowledge-documents` / `audit-assets` 桶（用户目录隔离）。
- 自检：`npm run typecheck`、`npm run test:server` 14/14。

## Phase 1｜知识库入库（Workflow C）— 集成测试通过（exit 0）

| 步骤 | 结果 |
| --- | --- |
| 创建私有知识库 | 通过（POST /knowledge-bases） |
| 签名上传（PUT） | 通过（createSignedUploadUrl，ASCII 存储路径；修复中文文件名 InvalidKey） |
| 提交文档 | 通过（校验意图/对象大小 → 先建 knowledge_ingest job 再建文档行） |
| Worker 入库 | 通过：下载 → checksum/大小校验 → 解析（txt/pdf/docx）→ 切块（800 字+10% 重叠）→ chunks 落库 → `ready` |
| 知识库状态 | 通过：`ready_count=1`、`selectable=true`、document_count 正确 |

## Phase 2｜广告法公共库

- seed 脚本入库《广告法》核心条款（第四/五/八/九/十一/十六/十七/二十八条），按"条"切分（source_locator=条款号），公共库 ready + selectable。
- 边界：为便于验证的核心条款子集，**完整官方文本待用户提供后替换/补全**（checksum 去重升级）。

## Phase 3｜内容审核（Workflow D，规则匹配 v1）— 集成测试通过（exit 0）

测试输入：正文"全网最低价、顶级品质，一针见效，根治色斑，100% 有效，研究显示效果显著。"

| 项 | 结果 |
| --- | --- |
| 草稿 → 提交 | 通过（先建 content_audit job 再转 queued） |
| 规则匹配 | 3 条风险：绝对化用语（high，第九条）、医疗功效断言（high，第十六条）、引证无出处（medium，第十一条），均含原文片段/依据条款/修改建议 |
| overall_risk 合成 | `high`（代码合成，非模型返回）；risk_counts {high:2, medium:1}；建议"修改后重新送审" |
| 快照 | 公共库快照落库（触发器 enforce_audit_finding_source 要求快照先于发现项，已适配） |
| 图片 | 无视觉模型时产生 `IMAGE_TEXT_EXTRACTION_FAILED` 覆盖警告 → needs_confirmation 兜底 |

## Phase 4｜前端接入

- 知识库页：列表/新建/文档上传（sha256 + 签名 PUT + 提交 + 状态轮询）。
- 新建审核页：草稿 → 图片上传（≤9 张）→ 提交 → 跳结果。
- 审核结果页：风险明细/依据/建议/覆盖警告/人工复核提示（固定展示"AI 辅助审核，不构成法律意见"）。
- 审核历史页：任务列表 → 点击进结果。
- 真实账号渲染检查：四个页面全部正常，无页面错误。

## 待用户验证（2026-08-19）

见工作台"下一步"待办清单。

## 边界（记入待确认）

1. 广告法公共库为核心条款子集，完整官方文本待补充。
2. 审核为规则匹配 v1（无 LLM/OCR）：图片文字未抽取、语义判断留接口；LLM 启用需 OPENAI_API_KEY 并经本地 Eval 校准（D005/Q003 边界）。
3. Embedding 未启用（Q003 延后），知识库检索为关键词/条款匹配。
4. 频控、批量、长期稳定性与多用户并发未验证。
5. 本结论不构成平台官方授权或生产稳定性承诺。
