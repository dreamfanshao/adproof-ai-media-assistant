# T04 工程初始化检查记录

> 日期：2026-08-16  
> 结论：Supabase 工程接入和远程数据库验证均通过

## 已完成

- 安装并精确固定 Supabase CLI `2.114.0`。
- 通过 `supabase init` 创建标准 `supabase/config.toml`。
- 项目标识设为 `adproof-ai-media-assistant`。
- Auth 本地站点和回调地址适配 Vite `5173` 端口。
- `public` 和 `graphql_public` 为 Data API 暴露 Schema；`private` 不暴露。
- 001–006 共 6 个 Migration 接入 `supabase/migrations/`。
- 工程 Migration 与 T03 已确认版本逐文件 SHA-256 一致。
- 创建不含真实账号或业务数据的空 `supabase/seed.sql`。
- 增加 start、stop、reset、lint、dry-run 和 migration list 命令。
- `npm run build` 通过，Vite 生产构建成功。
- npm 安装审计结果为 0 vulnerabilities。

## 远程验收结果

- 全新远程测试项目成功执行 001–006 Migration。
- 远程迁移记录与工程文件顺序一致。
- `public`、`private` Schema lint 无 error。
- RLS/Storage 与 Job fencing smoke test 通过，测试事务已回滚。
- 远程数据库类型生成、TypeScript 编译和生产构建通过。

详细记录见 `远程Migration与隔离测试记录.md`。

## 当前限制

本机没有 Docker Desktop、PostgreSQL 或其他容器运行时，因此无法执行本地 `supabase start`、`db reset` 和 `db lint --local`。

本次通过全新的远程空项目完整执行 Migration 验证空库初始化能力。不得对已有业务数据的项目执行 `db reset --linked`。

## 参考

- Supabase CLI：`https://supabase.com/docs/guides/local-development/cli/getting-started`
- Local workflow：`https://supabase.com/docs/guides/local-development/cli-workflows`
- Database migrations：`https://supabase.com/docs/guides/deployment/database-migrations`
