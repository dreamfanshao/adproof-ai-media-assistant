# 审核功能测试素材 · 违规图文（医美宣传）

> 用途：验证内容审核流水线的规则匹配与风险输出（测试专用，非真实发布内容）。
> 对应规则：`server/src/services/audit-rules.ts`（广告法规则匹配 v1）+ 小红书社区规范。
> 预期：应命中 4 类规则（绝对化用语 / 医疗功效断言 / 引证无出处 / 虚构用户评价），`overall_risk = high`。

## 一、图文标题（粘贴到"图文标题"）

```
光子嫩肤全网最低价，一针见效，三天根治色斑，史上最有效，100% 有效！
```

命中：`全网最低`（绝对化）、`一针见效`（医疗断言）、`根治`（医疗断言）、`史上最`（绝对化）、`100%`（绝对化）。

## 二、图文正文（粘贴到"正文"）

```
做完光子嫩肤第三天，皮肤真的白了一个度，效果立竿见影！
这是我用过史上最有效的项目，全网最低价，顶级专家操作，绝对安全，无副作用，一次就能彻底解决晒斑，根治不反弹，有效率高达 100%。
研究显示，超过 98% 的顾客在 7 天内看到明显改善。
现在加微信 xiaohongshu_test_888 领取优惠，前 50 名立减 500 元，好评返现 10 元。
```

命中规则与预期发现：

| 规则 | 触发词 | 等级 | 依据 |
| --- | --- | --- | --- |
| 绝对化用语 | 史上最有效 / 全网最低价 / 顶级 / 绝对安全 / 100% | 高 | 广告法第九条 |
| 医疗功效断言 | 一针见效 / 根治 / 无副作用 / 彻底解决晒斑 / 有效率 | 高 | 广告法第十六/十七条 |
| 引证内容无出处 | 研究显示 | 中 | 广告法第十一条 |
| 虚构用户评价 | 好评返现 | 中 | 广告法第二十八条 |

预期输出：`overall_risk = high`，`risk_counts = {high: ≥2, medium: ≥1}`，建议动作"修改后重新送审"。

## 三、图片（选填，上传配图测试"待人工复核"）

- 文件：`审核测试_违规图文_配图.png`（同目录）
- 预期：无 OCR 时结果页出现 `IMAGE_TEXT_EXTRACTION_FAILED` 覆盖警告 → 结果含"图片待人工复核"提示；若有图片未抽取文字，`overall_risk` 可能兜底为 `needs_confirmation`（当前无 high 以外兜底逻辑会先取最高风险 high，仍为 high）。

## 四、同时违反的小红书社区规范（辅助参考，不在广告法规则内）

- 3.1.3 发布售卖高风险类内容（医美整形）
- 3.2.1 导流到个人联系方式（加微信）
- 3.1.2 虚假宣传 / 价格欺诈
- 4.1.3 标题党 / 图文不符

## 五、测试步骤

1. 打开 `/audit/new`，粘贴标题与正文 → 开始审核。
2. 观察结果页：风险明细（等级/依据条款/修改建议）、总体结论、历史页记录。
3. 可选：上传配图 PNG → 结果页应出现"图片待人工复核"覆盖提示。
