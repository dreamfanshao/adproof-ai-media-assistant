# 2026-09-01 工作日志

## 达人检索单硬条件放宽与评分取 20

- 将候选策略从“所有业务条件均可用评分补偿”改为：统一统计粉丝、近期点赞、近 30 天发帖数、最近发帖时间四类硬条件失败数，只放宽淘汰人数最多的一项；其他硬条件及内容语义继续严格校验。
- 通过门槛的候选按综合评分降序取前 20；放宽条件未达会被降权，并在候选证据中标注本批放宽项。
- 关键词顺序改为按主题轮转，首轮至少覆盖 8 个不同主题页，避免单一医美项目的早期结果垄断候选池。
- 检索会话策略升级为 `redfox-realtime-v3`，沿用项目级历史去重，同时为新关键词与新筛选策略打开新的会话游标。
- 新增“只放宽一个条件”“其余条件严格”“首轮主题覆盖”“评分取前 20”等回归测试。
- 验证结果：全量测试 64/64 通过，`npm run validate` 通过，生产构建通过；Vite 仅保留既有的单 chunk 大于 500 kB 警告。
- 启动唯一新版 Worker（PID 21052），已接管此前失联的检索任务；运行时确认使用用户级 RedFox Key 短指纹和实时检索端点，未记录原始 Key。最终版本待当前真实任务完成后安全重载，避免中途重试造成重复消耗。
