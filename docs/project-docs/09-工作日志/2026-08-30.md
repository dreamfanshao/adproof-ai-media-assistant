# 工作日志｜2026-08-30

- 完成 Planner/Agent 管理面运行时一致性修复：
  - 修复 Planner System Prompt 与 Tool 分组乱码，Provider/模型名称统一读取 `agent/data/model-config.json`。
  - 新增生产能力清单 `agent/capability-manifest.ts`，Planner 预览直接镜像生产流水线，不调用 RedFox、不写数据库、不构造静态候选。
  - 达人 Skill 扩展为 13 个、审核 Skill 扩展为 10 个；Tool 分别为 11 个和 6 个，并统一旧、新注册表数据源。
  - 达人 Agent 增加个人账号识别、本人经历证据、语义匹配、证据落地和续检控制；三项语义判断共用一次模型调用，避免增加 RedFox 和 LLM 消耗。
  - 内容审核增加声明提取、知识覆盖、证据绑定、确定性风险下限和改写建议；可合并的判断共用模型请求。
  - 达人 Planner 的 `temperature/maxTokens` 已写入统一模型配置并进入真实模型请求；审核为确定性编排，页面不再展示无效 Planner 参数。
- 清理不真实管理数据：达人 Agent 控制台仅复用真实已落库 RedFox Agent Run；当前 Eval 样本明确标注为 fixture；历史乱码批次保留但从当前指标隔离；根路径改为当前管理首页。
- 新增运行时一致性测试与 Playwright 全页面自测：所有管理页无可见乱码、无 `console-c-*` 假候选、无旧 `Rnote API` 标识；浏览器控制台无错误。
- 验证：Server 14/14、Worker 9/9、Eval 5/5，合计 28/28；`typecheck`、lint、生产构建均通过。构建仅保留既有的单 chunk 超过 500KB 警告。
- 排查到任务 `8d002431-5c51-4fcf-a7b9-4d198e673296` 为僵尸任务：状态 `running/queued/0%`，锁已过期，已重试 12 次但上限为 3 次且没有事件。已修复 Worker 无限重领：领取时校验 `max_attempts`，执行异常即时记录并按上限收口，过期超限任务自动标记 `WORKER_RETRY_EXHAUSTED`。
- Eval 服务已重启加载新版本。共享 Worker 重启会触发数据库任务收口并可能领取其他排队任务，因安全审批要求，等待用户明确授权后执行。
