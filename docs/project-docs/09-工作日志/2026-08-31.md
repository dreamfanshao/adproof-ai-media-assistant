# 工作日志｜2026-08-31

- 排查最新同条件检索 `2398c2aa-9070-4e39-b5db-12e6c66c5325`：任务在 13 秒内以 `0/20` 结束，Job 结果显示 `requestCount=0`、`requestCountByEndpoint={}`、`notes_collected=0`，证明未调用 RedFox；唯一外部调用是 DeepSeek 意图结构化。
- 根因：Worker 进程创建于 2026-08-30 08:27，而关键词/自动续检源码修改时间晚于该进程；旧 Worker 仍将检索计划缩为 8 个基础词。复用的会话游标已将这 8 个词标记耗尽，因此直接误判 `sourceExhausted=true`。
- 完成修复：
  - 业务层固定批次目标为 20；DeepSeek 即使返回 `limit=50`，运行时也强制归一为 20。
  - 意图 Skill 提示词和输出契约改为 `limit=20`，持久化 Skill 升级为 v6。
  - 检索策略扩展为最多 32 路稳定关键词，每个医美/皮肤主题包含基础、个人分享、恢复/日记以及真实测评/改善前后兜底。
  - 确定性查询词始终保留，模型只能补充，不能把 32 路缩回已耗尽的旧词集合。
  - 旧 8 词游标会自动识别新增的第 9–32 路词并继续分页，不重置项目级达人去重历史。
- 新增原始检索词专项回归：输入用户本次完整文案、模拟模型 `limit=50` 和旧 8 词全部耗尽，断言生成 32 路词、会话重新打开且下一批实际选择未耗尽长尾词。
- 验证：全量测试 43/43（Server 16、Worker 22、Eval 5）、类型检查和生产构建通过；构建仅保留既有单 chunk 超过 500KB 警告。
- Worker 已于 2026-08-31 00:25 重启为 PID 28612，重启前确认共享队列无 queued/running 任务；新 Skill 配置和 32 路策略已通过运行时 API 核验。

## RedFox API Key 用户级替换

- 根因确认：原 Worker 只在进程启动时读取 `.env.local`，所以运行期间替换系统 `REDFOX_API_KEY` 不会进入后续任务；前台也没有用户自有 Key 的安全配置入口。
- 新增 `GET/PUT /api/v1/redfox-credential`：鉴权后仅返回配置来源、短指纹和更新时间，不回显原始 Key。
- 新增 `private.redfox_api_credentials`：按 `auth.users.id` 隔离；密文、随机 nonce、GCM tag 分列保存；撤销 `anon`、`authenticated` 的表权限。迁移 `202608310001` 已推送并确认本地/远端一致。
- Worker 每个新达人任务重新读取 `.env.local`，并按“用户级 Key > 最新系统 Key”解析；Key 不进入 job payload、结果或日志。
- 找达人页新增 Key 状态、配置/替换弹窗和余额不足快捷入口；弹窗关闭或保存后立即清空输入。替换后再次点击顶部“开始检索”，沿原游标和项目历史排除规则继续。
- 验证：全量自动化 48/48、`npm run validate`、生产构建均通过；Playwright 模拟回归覆盖配置、替换、余额不足入口、输入清空，确认外部 RedFox 请求为 0。API 健康检查 200、新路由未鉴权检查 401；更新版 Worker 已启动。

## RedFox 余额误报与重复 Worker 修复

- 最近三次失败任务均只调用一次旧 `story/api/xhsUser/searchArticle`，RedFox 原始响应为系统 Key 剩余积分 0.0；个人 Key 在任务创建前 5 秒已加密保存，数据库指纹与解密指纹一致且不同于系统 Key。
- 同一个人 Key 指纹使用实时 `story/api/xhs/ability/searchWork` 单请求直连成功，证明新 Key 本身有效。
- 根因：本机残留 PID 28612（00:25 启动）的旧 Worker，同时又运行新版 Worker。旧实例未加载用户级 Key 与实时端点代码，却仍在共享数据库中抢领任务。
- 修复：活跃任务数为 0 后停止全部旧/重复 Worker，仅启动 PID 8024；新增 PostgreSQL advisory lock，重复实例实测输出提示并以退出码 0 立即结束。
- 任务诊断新增运行时版本、Key 来源、不可逆短指纹和实际端点，不记录原始 Key；新增脱敏 Key 路由与单请求探测脚本。
- 验证：Server 21、Worker 33、Eval 5，共 59/59 通过；`npm run validate`、生产构建通过。真实 RedFox 单请求使用个人 Key + 实时端点成功；API/前端健康检查均为 200。
