# 工作日志｜2026-08-18

- 将 Codex 中 2026-08-11 至 08-18 的「媒介助手」主线对话导入当前工作区：解析 9 个会话 rollup 为可读转录（`.codex-import-tmp/out/`），覆盖产品画布、七步成诗 PRD、Figma 设计、技术方案/API 契约、数据库与 Supabase 实施、真实扫码 PoC 全部过程。
- 生成交接文档 `02-任务/01-current/T06-小红书扫码与真实读取PoC/工作产出/Codex会话导入与开发交接.md`（含决策摘要 D001–D013、时间线、代码/运行状态、剩余工作与安全备忘）。
- 核实接续基线：API `:3001` 与前端 `:5173` 健康（200/200）；`npm run test:server` 9/9 通过；`XHS_SESSION_ENCRYPTION_KEY`（32 字节）已本地生成，未写入任何文档。
- 确认 T06 唯一阻塞点：`.env.local` 缺少 `SUPABASE_SERVICE_ROLE_KEY`（+ 加密密钥必须成对配置），需用户从 Supabase 控制台提供；CLI 未登录、转录/缓存中均无可用密钥，无法自动获取。
- 安全提醒已转达：旧版 anon/service_role key 曾被 CLI 意外显示，建议用户在控制台 Legacy API Keys 中停用。
- 安装 modlens 视觉插件（本会话工具能力，与项目代码无关）：CLI `@liustack/modlens@3.18.3` 全局安装并进入 PATH；技能安装至 `~\.agents\skills\modlens\`（含 Windows 启动器）；provider 配置为 OpenAI 兼容路线 → DashScope `qwen3.7-plus`（key 存于 `~/.modlens/config.json`，权限 0600，不入项目文档）；测试图中文+英文 OCR 验证通过。用法：`modlens -i <图片路径>` 输出结构化 JSON 证据。注意：本机 Claude Code 路由到 DeepSeek 纯文本模型，`claude-cli` 视觉路线不可用。
- 前端改进（用户反馈"不要跳新页面扫码"）：找达人页点击"开始检索"且无有效小红书会话时，不再跳转 `/connect` 页面，改为**在当前页弹出内联扫码面板**（Modal 复用 `.qr-panel` 样式）；二维码直显本页，扫码连接成功后自动关闭并继续检索；手动关闭时会 DELETE 断开清理隐藏浏览器。`/connect` 路由保留供直达。`src/pages/creators-page.tsx` + `src/styles.css` 修改，`tsc --noEmit` 与 `npm run build` 均通过。
- 扫码无弹窗改进（用户反馈"不要弹出影响使用"）：① 反自动化加固——受控 Edge 增加 `--disable-blink-features=AutomationControlled` 等启动参数、屏蔽 `navigator.webdriver`、设置 Asia/Shanghai 时区，降低被平台风控识别概率；② 平台安全验证（restricted）时**不再自动弹出 Edge 窗口**，改为页面内提示 + 新增 `POST /api/v1/platform-connections/xiaohongshu/verify` 接口，由用户点击"打开验证窗口"按钮才弹出；③ 受限判定收紧为"风控文案 + 二维码不可见"双条件，风控解除后自动重新抓取二维码回页面。涉及 `server/src/services/xhs-connection-service.ts`、`server/src/routes/platform-connections.ts`、`src/pages/creators-page.tsx`、`src/pages/connect-page.tsx`、`server/test/app.test.ts`（新增 2 条 verify 路由测试）；类型检查、服务端测试 14/14、生产构建均通过。
- DSH 设置：`~/.dsh/settings.yaml` 新增 `llm-pi-ai.providers.deepseek` 路由（apiKeyEnv: DEEPSEEK_API_KEY，取自 `~/.dsh/.credentials.yaml`），用 `modelOverrides` 把 deepseek-v4-flash / deepseek-v4-pro 的 `input` 覆盖为 `[text, image]`，使 pi-ai 路由的 DeepSeek 模型可在界面传图；并将 `agent-default-model.provider` 从 `deepseek-official` 改为 `deepseek`（新会话默认即 pi-ai 路由，支持图片；已开会话仍绑定旧路由，需新会话生效）。注意：① 首次尝试用 `defaultInput: [text,image]` 无效——pi-ai 目录已声明 v4 模型 `input: ["text"]`，defaultInput 只是目录未声明时的兜底不覆盖声明；② 路由 `deepseek-official` 归 llm-deepseek 适配器（其模型 schema 无 input 字段），无法声明图片，故改用 pi-ai 目录路由名 `deepseek`。
- 已通过 host RPC（`POST /api/session.selectModel`，body `{"type":"client-request",...}`）把**当前会话** `session-36311911-3fbf-43a2-b9ee-499aaa41c31c` 的模型切到 pi-ai `deepseek` / `deepseek-v4-flash` / high（返回 ok:true），当前对话可直接传图；`/api/llm.providers` 已确认 `deepseek` 路由 active，`/api/llm.models` 显示两组 DeepSeek 模型。
- 接续 T06 受阻流程：核对 `.env.local` 已成对配置 `SUPABASE_SERVICE_ROLE_KEY` + `XHS_SESSION_ENCRYPTION_KEY`；恢复 API `:3001` 与前端 `:5173`（前端旧进程已死，由本会话后台任务重启），健康检查/代理链路 200，`test:server` 14/14。
- 定位"点检索未弹二维码"根因：`platform_sessions` 已存在用户有效 connected 会话（15:10 扫码建立）→ D013 按需扫码免扫码属正确行为，且 16:52 首次验证了跨重启恢复。
- 受控 Edge 窗口彻底隐藏（用户两次反馈"不要弹出"）：headless 实测被小红书风控拦截（300012）故保持有头；移除会取消最小化的 CDP `setWindowBounds("normal")`；新增浏览器级 CDP `SystemInfo.getProcessInfo` 取主进程 PID + user32 `ShowWindow(SW_HIDE)`，启动前先跑隐藏监控（按 `playwright_chromiumdev_profile` 临时配置目录匹配）；实测 100ms 采样 40 次窗口从未出现（`MainWindowHandle` 恒为 0），屏幕与任务栏均不可见；仅风控时用户主动点"打开验证窗口"才临时显示。
- `openLoginPanel` 加固：增加二维码等待、Esc 关闭弹窗遮罩、回车/点击重试，解决"弹窗遮罩拦截点击"偶发失败；二维码抓取连测 2/2 成功。
- 前端改进（用户反馈"右上角双按钮太丑 + 要能退出小红书"）：连接后右上角显示**小红书头像**（登录页 DOM 抓取、restore 重抓、失败回退默认图标），点击头像确认后退出（DELETE 连接 + 清 `platform_sessions`）；删除"退出小红书"文字按钮。
- 二维码等待优化（用户反馈"等待时间太长"）：`create()` 异步化——POST 秒回（实测 35–48ms，原约 5s），前端立即弹窗显示"正在生成二维码…"，就绪后 1s 轮询展示；捕获上限 12s 兜底；面板打开超时收紧。
- **T06 四步闭环用户真实扫码验收全部通过**（2026-08-18）：首次/补扫扫码连接、服务重启免扫码、重新登录免扫码、过期补扫（置 `session_expires_at` 过期后自动补扫成功）；受控 Edge 全程零窗口。验收证据补充至 T06 `验收证据/真实扫码与页面读取记录.md` 附录；决策账本追加 **D014**；工作台状态与索引更新；T06 任务包归档至 `02-任务/99-done/`。
- 全程验证：`test:server` 14/14、`npm run typecheck` 通过；QR 生成、恢复、补扫各路径均未在文档/日志输出 Cookie、Token、二维码内容或完整账号标识。
- T07 达人检索主链路建立（用户确认"建立完整 T07，按序推进"）：任务包 `02-任务/01-current/T07-达人检索主链路/`（四阶段：真实读取与字段映射 → 两步检索 → API/前端接入 → 去重与导出）；工作台/索引切换至 T07。
- T07 Phase 1：真实会话 DOM 诊断固化选择器证据（`工作产出/搜索页与达人主页DOM字段映射证据.md`）：搜索卡片 `a.author[href*="/user/profile/"]`（跳过"我"卡片）、主页 `.user-nickname`/`.shows` 指标/小红书号/IP属地/简介/头像/笔记列表。
- T07 Phase 1 字段映射模块完成并实测：`worker/src/xhs-constants.ts` + `worker/src/xhs-field-mapper.ts`（搜索收集 `extractSearchCreatorRefs`、主页字段 `extractProfileFields`、指标解析 `parseCountLabel`、风控判定）；真实会话集成测试 38 个搜索引用 + 2 个主页完整字段（粉丝/获赞与收藏/关注/小红书号/IP属地/简介/头像/12 条笔记，`completeness=complete` 零警告）；测试工具保留 `worker/src/poc/xhs-field-mapper-test.ts`。期间 vite 因编辑工具残留临时目录触发 EBUSY 崩溃一次，已清理临时文件并重启。
- T07 Phase 1 基础设施缺口（Q004）：`private` schema 不暴露给 PostgREST，Worker 需数据库直连；用户提供数据库密码后 `DATABASE_URL` 写入 `.env.local`（gitignore），安装 `pg`，vite.config 增加 watch 忽略（worker/**、.tmpdir/**）避免 EBUSY 崩溃。
- T07 Phase 1 Worker 检索执行器完成并通过 E2E：`worker/src/xhs-db.ts`（pg 直连：FOR UPDATE SKIP LOCKED 领取、租约恢复、单事务 job_events 回写）+ `xhs-search-executor.ts`（会话校验→搜索收集→主页串行限速映射→粉丝硬过滤→去重→creators/project_creators 落库→search_tasks/job_events 回写）+ `worker/src/index.ts` 守护进程；发现并适配 `search_tasks.task_id` 外键引用 `private.jobs.id`（先建 job 再建 search_task）。E2E 实测 3 轮全部 completed（收集 34–38、落库 3 人、过滤/去重正确、清理干净）；守护进程 `worker-*` 运行中。
- T07 Phase 2 语义匹配完成并通过 E2E：`worker/src/xhs-semantic-matcher.ts`——启发式语义条件判定（bio+笔记标题中文子串匹配，any/all/none）、确定性活跃度与 `match_score` 合成（语义 50%+活跃 25%+证据 25%，符合技术方案 §13.3"模型不返回排序分"）、`evidence_summary`/`analysis_json`/`creator_evidence` 落库、search_tasks 版本记录；E2E 实测"白日做梦师"命中"光子嫩肤/医美"→ matchScore 65 > 未命中 40；LLM 判定路径（OPENAI_API_KEY 可选）留接口。守护进程与 E2E 抢 job 问题：测试前需停守护进程。
- T07 Phase 3 后端 API 完成并通过集成测试：`server/src/services/rule-parser.ts`（search-rules:parse 启发式）、`creator-search-service.ts`（pg 建 job + supabase 建 search_task，先 job 后 task）、路由 projects/search/creators/task-events（SSE）、app.ts 挂 pg Pool（DATABASE_URL 门控）；修项目创建漏 `idempotency_key`、`require` 与 ESM 冲突（改静态导入）；集成测试全链路通过（项目→解析→建任务→执行器→达人列表→PATCH→SSE），测试工具保留 `worker/src/poc/xhs-phase3-api-test.ts`；API(3001)+Worker 守护进程均重启运行。
- T07 Phase 3 前端真实接入完成：找达人页切真实 API 全流程（自动建默认项目→解析条件→建检索任务→2s 轮询进度→真实候选表格（match_score 降序/头像/主页链接）→选入/弃用/恢复 PATCH→重复数展示）；`Creator` 类型扩展 profileUrl/avatar，表格支持头像与真实主页链接；typecheck 通过，vite HMR 正常。
- T07 用户实测发现并修复两个 bug：① Worker 守护进程崩溃——`private.jobs` 无 `task_id` 列，守护进程 `job.task_id.slice` 崩溃导致任务卡在 validating_session；改为日志用 `payload.search_task_id`。② 建任务撞 `search_tasks_one_active_per_user_idx` 唯一约束（每用户仅一个非终态任务）→ `createSearchTask` 自动取消既有进行中任务再新建；执行器启动时检查 `cancel_requested` 提前结束。另：前端挂载时加载项目已有候选（历史结果即时可见）；手动清理卡住的遗留任务后，过期锁 job 被新 Worker 租约恢复自愈重跑，任务 `partial` 完成、**41 个真实达人落库**（不足 50 因数据源耗尽，符合设计）。
- T07 头像问题修复（用户反馈"头像不显示"）：根因是头像抓取用 `img[src*=avatar].first()` 抓到页面第一个头像（登录用户头部头像/默认占位），41 人全部落到同一默认头像 `645b8006…`。修复：worker `scrapeProfileAvatar` 改为**昵称元素向上定位用户信息区**取主页主人头像（兜底页面最大头像），并排除默认头像哈希；服务端 `scrapeAvatarUrl` 同步排除默认哈希。清空 41 个错误头像后后台重抓 **41/41 成功（真实互异头像）**；API 重启后 restore 会用新选择器重抓登录用户头像（顶部头像修复）。typecheck 通过。
- T07 顶部登录头像再修复（用户反馈"右上角头像仍不显示"）：诊断发现该账号本身即小红书默认头像（主页昵称"小红薯6A7685E8"、头像 `645b8006…`），此前"排除默认头像"规则把本人默认头像也排除了 → 返回 null。修复：抓**登录用户本人**头像时不再排除默认头像——优先取顶部"我"导航链接头像（=本人），兜底访问本人主页按昵称定位抓取；API 重启后 restore 重抓生效。typecheck 通过。
- T07 Phase 4 完成：① Excel 导出——`creator-export-service.ts` + `GET .../creators/export.xlsx`（xlsx 库，10 列：昵称/小红书号/粉丝/活跃度/匹配分/状态/主页链接/匹配理由/获取时间/更新时间，?status= 过滤；中文文件名用 filename* 编码解决 Node 非 ASCII header 报错）；集成测试 41 行完整。② 前端共享模块 `src/lib/project-api.ts` + `src/state/project-creators-hook.ts`，找达人/已选/已弃用三页切真实 API（含过滤视图状态联动），导出按钮三处走真实接口。typecheck 通过、test:server 14/14。
- T07 前端"其他页面全空白"问题排查（用户反馈）：用真实测试账号（service role 创建，测后删除）+ Playwright 复现，定位为**运行数小时的 vite 开发服务器模块缓存过期**（`CreatorExportButton is not defined` 等陈旧模块错误）；重启 vite 后全部路由恢复正常渲染，无页面错误。测试账号创建/清理流程复用可行。
- T07 验收收尾：写 `验收证据/达人检索主链路验收记录.md`（四阶段验证 + 用户真实使用 41 人落库 + 边界）；任务说明置"已完成"；决策账本追加 **D015**；工作台/索引更新；T07 任务包归档至 `02-任务/99-done/`。
- T08 建立并完成 Phase 1（知识库入库）：任务包 `T08-知识库与内容审核主链路/`（四阶段：知识库入库/广告法公共库/审核流水线/前端接入）；`server/src/routes/knowledge.ts` + `knowledge-service.ts`（签名上传 + upload_intents + 先 job 后文档行）、Worker `knowledge-ingest-executor.ts`（下载/校验/解析 pdf-parse+mammoth/切块/状态机）+ 守护进程支持 knowledge_ingest；E2E 通过（建库→PUT 上传→提交→ready，chunk 落库，selectable=true，exit 0）；修复中文文件名 InvalidKey（ASCII 存储路径）、pg bigint 字符串比较、knowledge_documents 无 error_message 列；装 pdf-parse/mammoth。typecheck/test:server 通过，API(3001)+Worker(worker-12088) 运行中。
- T08 完成 Phase 2/3/4（用户指示"执行完所有待办，先不做 eval，需验证的先跳过，明日验证"）：① 广告法公共库 seed（核心条款按条切分入库）；② 审核流水线——audit 路由/服务（草稿/图片上传/提交/查询）、audit-rules（规则匹配 v1：绝对化用语/医疗断言/引证无出处/虚构评价）、Worker content_audit（快照先于发现项适配触发器、overall_risk 代码合成）；审核 E2E 通过（high、3 风险项、快照、exit 0）；③ 前端知识库/新建审核/审核结果/审核历史页全部切真实 API，真实账号渲染检查无页面错误。写 `验收证据/知识库与内容审核验收记录.md`（草稿，待用户验证）。修复：快照先于发现项（enforce_audit_finding_source 触发器）、audit_tasks 无 error_message 列。
- 交付测试素材：抓取小红书社区规范生成 Word（`01-输入素材/02-法规与规则/小红书社区规范.docx`，七章结构化，已登记素材清单）；生成违规图文测试包（`03-测试数据/审核测试_违规图文.md` + 配图 PNG，可触发 4 类广告法规则）。
- 用户实测审核后修复两个前端问题：① 审核结果页提交后立即打开显示"0 风险/需人工复核"（Worker 未完成时取到中间态）→ 结果页改为**每 2 秒轮询至终态**，未完成显示"审核中…"进度而非误导性结论；② 新建审核图片上传区**支持拖拽**（onDragOver/onDrop，限 JPG/PNG ≤9 张）。typecheck 通过。
- T08 验收收尾（2026-08-19 用户真人验证通过）：验收记录置"已验证"；任务说明置"已完成"+验收结论；决策账本追加 **D016**；T08 任务包归档至 `02-任务/99-done/`；工作台/索引更新（T01–T08 全部归档，MVP 两大主链路完成）。
