# 工作日志｜2026-08-19

- 应 2026-08-18 计划：T08 用户真人验证通过 → 追加 D016、归档 T08（T01–T08 全部完成）。
- 建立独立本地 Eval 系统（D005：仅本地、独立端口、不部署）：`eval/` 目录（datasets / runner / server / public 看板），`npm run eval:server` 启动于 **http://127.0.0.1:3081**，与主系统（3001/5173）完全隔离。
  - 三套套件：rule_parse（检索规则解析黄金集 10 例）、audit_rules（审核规则召回标注集 12 例，含精确率/召回率）、semantic_match（真实库达人语义匹配基线，期望集待人工标注回填）。
  - 首轮 24 例通过 20（83.3%）→ 修复 rule-parser 三处真实缺陷后 **24/24（100%）**：①"不超过"误匹配 lt（应 lte，调整比较符优先级）；②"粉丝超过5000的医美达人"混合子句丢失语义词（硬条件处理后剩余文本继续走语义词提取）；③"不要医美达人"否定语义未处理（新增 match:none 否定前缀支持）。
  - audit_rules 12/12（精确率/召回率 1.0）；semantic_match 为基线套件（展示关键词基线 vs 启发式判定命中集，期望集待人工标注）。
- 解析器修复同步生效于主系统（API tsx watch 热载），`test:server` 通过、API/前端健康。
- Eval 升级为「运营与质量中心」（参考用户提供的 Agent Operations 参考图）：`eval/` 新增 storage.ts（JSON 持久化：annotations/suggestions/settings）、runner 扩展（评分方法标注：精准匹配/余弦相似度（字符 bigram）/LLM-as-judge（OpenAI 兼容，prompt 可编辑，未配置 Key 显示 pending）/人工评估）、server 新增数据集/评测/标注/建议/设置/导入导出/人工评估路由，看板六页签（概览/评测集/评测中心/数据标注/人工评估/改进建议/评分方法设置）。
  - 数据标注：总数/待标注/已审核/已驳回统计卡 + 六维标注（正确性/相关性/完整性/安全性/语气/整体质量）+ 状态流转 + 保存全部 + JSONL 导出（全部/已审核）+ 导入 Bad Cases + 从语义评测生成待标注项。
  - 评测中心：逐用例展示评分方法、期望 vs 实际、Bad Case 高亮并可一键导入标注；改进建议中心（自动建议 + 人工建议 + 状态）。
  - 冒烟验证：标注新增/保存/建议/JSONL 导出/列表全部通过；数据已清理。
  - 端口不变：http://127.0.0.1:3081（`npm run eval:server`）。
- DSH→Codex 交接：写 `00-工作台/DSH会话交接_2026-08-19.md`（本会话 08-18~19 完成事项/产物/运行环境/未做待办/Codex 读序）。DSH 会话存储为 zstd 压缩 JSONL（`~/.dsh/sessions/--E-code-AICoding-agent_medium--/session-140664c1…/session.jsonl.zstd`），本机无 zstd CLI/python，WASM 解码器（zstddec/zstd-codec）对 DSH 格式解码异常，故以文档交接为主通道（与 Codex→DSH 交接同构），完整逐字转录需先装 zstd 工具。
