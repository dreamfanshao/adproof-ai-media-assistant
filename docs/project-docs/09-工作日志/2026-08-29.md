# 工作日志｜2026-08-29

- 排查 2026-08-29 00:00 发起的达人检索：RedFoxHub 返回 60 篇笔记、21 个唯一账号，但单一长关键词召回偏机构内容；作品列表字段错误复用了搜索种子笔记；“近三个月点赞”误用总互动量；账号详情失败和各阶段淘汰原因未在页面展示，最终 0 个候选。
- 完成检索链路修复：
  - 将医美项目和皮肤问题拆为最多 8 路个人经历型关键词（“体验 日记”/“改善 记录”），总抓取页数保持有界并跨关键词去重。
  - 固定检索执行计划，移除 LLM Planner 的 30 秒阻塞；图片分析失败不再覆盖已经完成的文字语义判断。
  - 修正 RedFox 作品列表映射，每篇作品使用自身标题、发布时间、图片、点赞和互动；近期点赞硬条件只读取点赞数。
  - 账号详情、作品列表和语义分析失败均显式记录原因，不再静默吞错。
  - 新增笔记数、账号数、资料失败、粉丝过滤、点赞过滤、语义过滤、分析异常、已有重复、本次新增九项漏斗统计，并在找达人页面实时展示。
- 数据库新增 `search_tasks.stage_counts` JSONB 字段；迁移 `202608290001_creator_search_stage_counts.sql` 已推送至远程 Supabase，并确认本地/远程迁移版本一致。
- 验证结果：Worker 专项测试 3/3 通过；Worker 严格类型检查通过；项目 `typecheck`、`lint`、生产构建通过。全量 Server 测试中 11/14 通过，剩余 3 项为既有小红书会话接口返回 409 的失败，与本次达人检索改动无关。
- 使用原始条件完成真实回归任务 `39fa64e2-58b5-4ef3-ad67-e42da9c8f6f7`：8 路关键词召回 144 篇笔记、105 个唯一账号；系统采用 4、明确过滤 81、因 RedFox 积分耗尽未完成资料判断 20。
  - 首次回归发现“单路关键词未收录会终止整次检索”，已改为记录 `keywordFailures` 并继续其余关键词；专项测试增至 4/4 通过，Worker 严格类型检查通过。
  - 三路关键词部分页未收录（晒斑、雀斑、超声炮第 2 页），其余分路仍正常返回结果。
  - 系统采用项中“张小熊西安医研团”为机构账号，模型理由与采用结论自相矛盾；按个人博主口径人工复核应过滤，可信采用为晴天娃娃、切梨子、顾清寒 3 个。
  - 本次共调用 RedFox 327 次；当前先对全部账号拉取详情和作品后再过滤粉丝，导致积分消耗偏高，后续应将账号资料、粉丝硬过滤、作品列表拉取拆成分阶段执行。
- 完成 RedFox 调用消耗优化：
  - 调用顺序改为“项目内已有达人批量去重 → 账号详情 → 粉丝硬过滤 → 种子笔记点赞判断 → 必要时作品列表 → 语义分析”；粉丝不合格不再调用笔记详情和作品列表。
  - 搜索结果已有正文、图片、点赞和发布时间时直接复用；仅缺关键内容时补查笔记详情。每个账号选择近期、高赞且带个人体验信号的代表笔记。
  - 种子笔记已满足近 90 天点赞门槛时不调用作品列表；只有活跃历史硬条件或种子点赞不足时才查询。
  - 搜索分页改为首轮覆盖全部主题、候选不足才追加最多 4 个第 2 页；未收录关键词不再继续翻页。
  - 增加进程内有界缓存：搜索 5 分钟、未收录结果 10 分钟、账号详情 6 小时、作品列表 30 分钟、笔记详情 6 小时；同一 Worker 内重复检索可大幅减少实际请求。
  - RedFox 余额耗尽或达到每任务 160 次硬调用上限后立即熔断，剩余账号标记待复核，不再重复请求；任务结果记录各端点调用数和缓存命中数。
  - 候选详情池限制为目标人数的 2 倍（最低 60），按代表笔记质量降序处理，达到目标即停止。
  - 按 2026-08-29 真实任务估算，首次同类检索由 327 次降至约 120 次，预计减少约 63%；短时间重复相同检索在缓存有效期内可进一步接近 0 次 RedFox 请求。
  - 验证：Worker 专项测试 6/6、Worker 严格类型检查、项目 validate 和生产构建全部通过；Worker 已重启加载新逻辑。
- 完成“每批 20 人 + 持久化续检”改造：
  - 新检索和继续检索的单批目标统一为 20 人；移除固定“目标 2 倍、最低 60/最多约 100 人”的候选池停止逻辑，改为达到 20 人、数据源耗尽或安全调用预算不足才结束当前批次。
  - 新增 `creator_search_sessions` 和 `creator_search_screenings`，按项目、规范化检索词和确认规则保存关键词游标、已处理笔记、已处理达人、采用/过滤/失败原因与冷却时间。
  - 同一检索条件再次运行会继续上次游标；已采用和项目已有达人永久排除，过滤对象冷却 30 天，临时失败对象冷却 1 天；历史任务首次续检时自动回填已过滤达人。
  - 页面新增“继续检索 20 人”；任务未满且仍有后续数据时设置 `can_continue`，继续接口绕过相同请求的 5 分钟防重但仍复用同一检索会话。
  - RedFox 单关键词分页上限扩为 10 页，并采用多关键词轮转；每次只取下一页并保存进度，避免为凑大池一次性拉取大量页面。
  - 迁移 `202608290002_creator_search_sessions.sql` 已通过 dry-run 并推送至远程 Supabase；完整测试 22/22、生产构建通过，Worker 已在确认无活动任务后重启。
  - 本轮仅验证流程和代码，未发起真实 RedFox 检索。
