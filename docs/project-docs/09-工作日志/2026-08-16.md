# 工作日志｜2026-08-16

- 用户确认 OpenAPI 3.1 API 契约与共享数据 Schema。
- 将 `媒介助手_MVP_API契约_v1.yaml` 和 `媒介助手_MVP_共享数据Schema_v1.md` 提升到技术文档 `01-current/`。
- 将完成验收的 T02 任务包归档到 `02-任务/99-done/`。
- 建立 T03“数据库与认证基础”任务包，开始数据库模型、Migration 和 RLS 设计。
- 完成 001–006 Migration、数据库/RLS 设计和双用户测试模板草案。
- 独立读者测试发现 Job 过期租约写回风险；已加入 `lease_version` fencing、续租及原子状态/事件迁移。
- 补齐知识入库单用户并发约束、公共广告法文档 nullable `task_id`、审核快照不可变、Storage upload intent 校验、同项目搜索任务复合外键和项目创建幂等持久化。
- 6 个 SQL 文件通过静态解析；OpenAPI 再次通过 lint，并生成最新 TypeScript 类型证据。
- 用户确认 T03 数据库与 RLS 方案，并确认 Embedding/ANN 延后决策及敏感数据只经 Fastify API/Worker 访问。
- 项目阶段由技术设计切换到 Supabase 实施、账号隔离和真实小红书链路验证。
- 安装并精确固定 Supabase CLI `2.114.0`，初始化 `supabase/config.toml` 和空 seed 文件。
- 将 001–006 Migration 接入工程 `supabase/migrations/`，增加本地 reset、lint 和远程 dry-run 脚本。
- 将 T03 归档并建立 T04“Supabase 实施与账号隔离”任务包；数据库设计提升为正式技术文档。
