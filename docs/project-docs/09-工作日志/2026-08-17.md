# 工作日志｜2026-08-17

- 用户创建 Supabase Free 测试项目 `medium-ai-dev`，区域为新加坡；CLI 已成功关联。
- 通过 Session Pooler 完成 dry-run，并按顺序成功推送 001–006 Migration。
- Docker 缺失只导致 CLI 无法缓存 migration catalog，不影响远程 Migration 执行。
- 远程迁移表已核对，001–006 六条记录完整。
- 远程双用户 RLS/Storage 隔离测试通过：私有业务数据和对象互不可见，公共广告法库可读不可写，浏览器角色不能访问 `private.*` 和 `platform_sessions`。
- Job fencing 测试通过：旧 Worker 的过期 `lease_version` 被拒绝，新 Worker 可原子完成 Job 和事件写入。
- 两组测试均使用事务并回滚；核对测试账号、项目、Job 和 Storage 对象残留为 0。
- 生成远程 `public` Schema TypeScript 类型，`npm run typecheck` 与 `npm run build` 均通过。
- 数据库 lint 覆盖 `public`、`private`，结果为 `No schema errors found`。
- T04 已归档，启动 T05“真实登录与 API 基础”。
- 前端已从演示登录改为 Supabase 邮箱密码注册/登录、Session 恢复、退出和业务路由保护。
- 新建 Fastify API，完成 `/api/v1/health`、`GET /api/v1/me`、`PATCH /api/v1/me`、Bearer Token 验证和 RLS 用户资料访问。
- 增加严格 CORS、安全响应头、请求体上限、基础限流、UUID request ID 和统一错误结构。
- 新增 5 个 API 注入测试，全部通过；TypeScript 检查与前后端生产构建通过。
- 未创建或修改真实 `.env`；待用户授权后配置浏览器安全的 Supabase Project URL 与 Publishable Key，并进行真实账号端到端验收。
- 用户授权创建 `.env.local`；文件只包含 Project URL 与 Publishable Key，并确认被 Git 忽略。
- 修复 Supabase 验证邮件本地回跳、Vite 配置优先级、`/api` 代理和服务端用户 Token 传递方式。
- 真实邮箱注册、验证、登录与 Session 恢复通过。
- Fastify `/me` 成功使用用户 Token 读取 RLS 保护的 `user_profiles`；用户确认侧栏资料同步正常。
- T05 自动测试 5/5、TypeScript 检查与最终生产构建通过，真实账号与 API 基础验收完成。
